services:
  - type: web
    name: bsnl-poynad-tip
    env: static
    buildCommand: echo "Building static site..." && ls -la
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /home.html
    envVars:
      - key: NODE_ENV
        value: production
